export type RepositoryType = {
  description: string
  id: string
  isPrivate: boolean
  primaryLanguage: {
    color: string
    name: string
  }
  name: string
  pushedAt: string
  url: string
  updatedAt: string
}
